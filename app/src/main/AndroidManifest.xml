<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.a97557.movie">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".LogoActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity"></activity>
        <activity android:name=".HomeActivity"></activity>
        <activity android:name=".StudioActivity"></activity>
        <activity android:name=".ScheduleActivity"></activity>
        <activity android:name=".PlayActivity"></activity>
        <activity android:name=".EmployeeActivity"></activity>
        <activity android:name=".PersonActivity"></activity>
        <activity android:name=".EmployeeInfoActivity"></activity>
        <activity android:name=".ModifyEmployeeActivity"></activity>
        <activity android:name=".InsertEmployeeActivity"></activity>

    </application>

</manifest>